import {
  afterEach, beforeEach,
  describe, expect, it, vi,
} from 'vitest';
// import { TestService } from './TestService';

// const testMethodMock = vi.spyOn(TestService, 'testMethod');

describe('Test', () => {
  afterEach(() => {
    vi.resetAllMocks();
  });
  beforeEach(() => {
  });

  it('Test something', async () => {

  });

});
